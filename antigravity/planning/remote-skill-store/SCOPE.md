# Scope

- Rename the Market tab and page to Skill Store / Skill 商店.
- Remove the bundled index and all built-in-directory search paths.
- Add ClawHub, SkillsMP, and skills.sh as the only selectable providers.
- Normalize remote results into the existing download queue.
- Keep existing local library, manual URL import, update, and installation behavior.
- Do not add API-key or Vercel OIDC configuration in this change.
